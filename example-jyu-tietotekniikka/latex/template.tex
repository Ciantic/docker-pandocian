% !TeX encoding = utf8
\documentclass[utf8,bachelor]{gradu3}
\usepackage{palatino} % Platino fontti, pakotettu kandidaatintutkielman ohjeissa
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{url} 
\usepackage{booktabs}
\usepackage{longtable}

% HUOM! Tämän tulee olla viimeinen \usepackage koko dokumentissa!
\usepackage[bookmarksopen,bookmarksnumbered,linktocpage]{hyperref}

\addbibresource{$for(bibliography)$$bibliography$$sep$,$endfor$}

\begin{document}

$if(title)$
\title{$title$}
$endif$

$if(title_eng)$
\translatedtitle{$title_eng$}
$endif$

$if(suuntautumisvaihtoehto)$
\studyline{$suuntautumisvaihtoehto$}
$endif$

$if(avainsanat)$
\avainsanat{$for(avainsanat)$$avainsanat$$sep$,$endfor$}
$endif$

$if(keywords)$
\keywords{$for(keywords)$$keywords$$sep$,$endfor$}
$endif$

$if(tiivistelma)$
\tiivistelma{$tiivistelma$}
$endif$

$if(abstract)$
\abstract{$abstract$}
$endif$

$if(contact)$
$for(contact)$
\contactinformation{$contact$}$sep$
$endfor$
$endif$

$if(author)$
$for(author)$
\author{$author$}$sep$
$endfor$
$endif$

$if(examiner)$
$for(examiner)$
\supervisor{$examiner$}
$endfor$
$endif$

$if(work)$
\type{$work$}
$endif$

\maketitle

\mainmatter

$body$

\printbibliography

\end{document}